"""
RL-агент для адаптивной маскировки трафика.

Структура пакета:
  env.py    — Gymnasium environment (RLProxyEnv)
  reward.py — функции вознаграждения (суррогатная + nDPI)
  agent.py  — обёртка для инференса в Flask
  train.py  — обучение (SB3 SAC)

Концепция:
  Supervised-трансформер обманывал суррогатный классификатор, но не реальный
  nDPI — потому что nDPI смотрит на протокол, а не на flow-статистику.

  RL-агент получает reward от реального nDPI (через периодическую валидацию),
  и учится находить такие комбинации (chunk_size, delay_ms, first_frag_size),
  которые путают именно DPI-систему реального противника.

  Во время обучения используется суррогатная награда (DPIClassifier) как fast
  path, и периодическая валидация с ndpiReader как slow path.
  Во время инференса (в Flask) обученная политика работает за ~1мс.
"""
